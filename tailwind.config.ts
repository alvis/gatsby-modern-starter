export const purge: string[] = ['./src/**/*.{js,jsx,ts,tsx}'];
export const darkMode: boolean | 'media' | 'class' = false;
export const theme = {
  extend: {},
};
export const variants = {
  extend: {},
};

export const plugins: string[] = [];
